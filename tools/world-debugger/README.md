# World debugger — Phase 3

Planned: procedural world graph, zone themes, blocked edges, three independent routes and seed replay. World generation is outside Phase 0–1; no empty implementation is exposed as a working tool.
